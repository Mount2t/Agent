### 大模型大在哪里

7B、14B、72B 这样的数字。这里的 B 是 Billion（十亿）的缩写，7B 就是 70 亿个参数，72B 就是 720 亿个参数。

### 核心概念

#### Token

Token 是模型内部的最小处理单元。

#### 上下文窗口（Context Window）

上下文窗口是模型一次对话中能看到的文本总量上限，**单位是 Token。**

#### Temperature：控制回答的创造力